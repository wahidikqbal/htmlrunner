name: Deploy HTML

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy via SSH
        run: |
          ssh deploy@172.21.21.109 "
            cd /var/www/html &&
            git fetch origin &&
            git reset --hard origin/main
          "
